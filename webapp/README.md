# Webapp

Run from the repo root:

```bash
python3 webapp/generate_pickle.py
streamlit run webapp/app.py
```

Optional refresh steps:

```bash
python3 build_simulator.py
python3 webapp/try_pickle_builders.py
```
